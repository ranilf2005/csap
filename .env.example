# ---------------------------------------------------------------------------
# Cisco Security Automation Platform (CSAP)
# Copy to .env and fill in. install.sh generates the secrets for you.
# NEVER commit the real .env file.
# ---------------------------------------------------------------------------

# --- Release / registry ----------------------------------------------------
CSAP_VERSION=0.4.0
REGISTRY=ghcr.io
IMAGE_NAMESPACE=ranilf2005

# --- Networking ------------------------------------------------------------
HTTP_PORT=80
HTTPS_PORT=443
PUBLIC_URL=https://localhost

# --- Secrets (REQUIRED - generate with install.sh) -------------------------
# openssl rand -hex 32
SECRET_KEY=
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CREDENTIAL_ENCRYPTION_KEY=
POSTGRES_PASSWORD=
REDIS_PASSWORD=

# --- First-run administrator ----------------------------------------------
# Password must be >= 12 chars. Change it after first login.
CSAP_ADMIN_EMAIL=admin@example.com
CSAP_ADMIN_PASSWORD=

# --- Database --------------------------------------------------------------
# DATABASE_URL and REDIS_URL are composed by docker-compose.yml from these
# values; set them here only if you run the backend outside Docker.
POSTGRES_USER=csap
POSTGRES_DB=csap

# --- Application -----------------------------------------------------------
LOG_LEVEL=INFO
ENVIRONMENT=production
ACCESS_TOKEN_EXPIRE_MINUTES=60
DATA_DIR=/data
BACKEND_URL=http://backend:8000
